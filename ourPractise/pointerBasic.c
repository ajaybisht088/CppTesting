#include<stdio.h>

void main(){
    int *p;
    // p = NULL;
    unsigned int x =10;
    // p = &x;
    // printf("\n &x = %u", &x);   // address of x => &x (decimal form)
    // printf("\n &p = %u", &p);   // address of p => &p (decimal form)
    // printf("\n p = %p", p);     // address of x => using %p with p (hexadecimal form)
    // printf("\n x = %x", x);
    // printf("\n &x = %d", &x);
    // printf("\n px = %d", p);
    // printf("\n px = %X", p);
    // printf("\n p = %d", p);

    // int i=85;
    // printf("%p %Fp", &x, &x);
}
